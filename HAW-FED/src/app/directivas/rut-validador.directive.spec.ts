import { RutValidadorDirective } from './rut-validador.directive';

describe('RutValidadorDirective', () => {
  it('should create an instance', () => {
    const directive = new RutValidadorDirective();
    expect(directive).toBeTruthy();
  });
});
